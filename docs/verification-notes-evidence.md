# 笔记与证据验证 · 2026-09-07

使用独立资料库 `F:/codex/tmp/paper-ocean-implementation-20260907/data`、公开 Attention Is All You Need PDF 和增加尾部注释后的另一份测试副本。后者产生独立内容 ID，用于核对引用身份。没有覆盖用户资料库。流式输出由本地模拟器生成并明确标注，不是模型质量验收。

## 已完成的界面验证

- 选取 PDF 同页两行文字、保存高亮、建立 Markdown 笔记；修正绝对定位文本层把换行英文粘连的问题，再次选取确认 `fully connected` 保留空格。
- 切换另一篇测试论文，向原笔记追加独立证据；磁盘记录包含两篇内容 ID 和各自页码/归一化矩形。
- 放大 PDF 后高亮仍落在原句上；关闭测试页面和服务后冷启动，高亮与两条笔记均恢复。
- AI 历史回答转为可编辑笔记，保存原讨论/消息 ID，回到原回答；归档和恢复笔记正常。
- 笔记公式预览正常；实际导出 ZIP，使用 Python `zipfile.testzip()` 和 Pillow 验证两张 PNG，无损坏。包内包含 `note.md`、`evidence.json`、两张按内容 ID/页码命名的 918×1188 页图；视觉核对为 PDF 第 3 页完整原文。
- 新建固定两篇论文的讨论，编辑标题、回答深度和提问模板；关闭其中一篇的阅读标签后，讨论仍绑定两篇原文。
- 发送要求核对第 3、5 页的问题，模拟服务接收到对应内容 ID 下的两个页图；界面显示全文文本 30/30 页和准确的页图标记。
- 同一组论文新建第二条讨论，历史为空且草稿独立，冷启动后恢复。
- 两个 Web 页面读取相同版本，A 保存后 B 修改被拒绝；A 的磁盘草稿不变，B 仍显示未保存文字，导出 JSON 包含 B 的草稿和笔记。

## 自动验证

68 项测试通过。新增覆盖：笔记/原消息/跨论文锚点恢复、缺失论文不抹除历史证据、几何边界、Markdown 引用与 AI 来源、截断 PNG 拒绝/缓存隔离、离线 PDF JavaScript MIME、资料库版本冲突及相同快照幂等重试、极小上下文预算显式拒绝。类型检查通过，生产构建通过；主包约 1.15 MB 的体积警告仍留待最终优化。

## 剩余边界

真实模型引用准确性、流式输出期间旧消息滚动、原生窗口关闭和中文输入法仍需最终验收。扫描文字的 OCR 尚未提供；页图失败不会谎称已提供图像。多论文取证的页图目前来自活跃论文，最多三页。笔记图片导出重新渲染与内容 ID 相符的原件，原件缺失/变化时显式停止；可单独导出 Markdown。高亮不是嵌入原 PDF 的标准批注。Web 冲突保护阻止覆盖，保留未保存副本，不做自动合并。
